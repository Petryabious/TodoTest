import { ModalBaseDirective } from './modal-base.directive';

describe('ModalBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalBaseDirective();
    expect(directive).toBeTruthy();
  });
});
